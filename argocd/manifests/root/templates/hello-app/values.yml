ingress:
  enabled: true
  className: nginx
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: ks.svc.cluster.local
      paths:
        - path: /
          pathType: ImplementationSpecific